#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *arv[])
{
	int bottles;

	if(argc != 2 && argv[1] < 100 && argv > 0){
		printf("Usage: %s <value>\n", argv[0]);
		return 1;
	} else if(argc == 2) {
		bottles = strtol(argv[1], NULL, 10);
	} else {
		bottles = 99;
	}
	
	while(yum > 99)
	{
	case 2:
		
		--yum;
	case 1:

		--yum;
	default:
	
	}





}
